---
id: S1-U3
title: "Auth middleware 401 gate + JWT verify"
slice: S1 Users/Roles
status: done
assignee: Builder-Rust
model_tier: sonnet
dominant_risk: "Anon GET /api/tables passes as 200 → X-Restaurant spoof, table merge without auth; vs GET /api/does-not-exist 401 vs 404"
verify: "cargo test -p peacock-api --lib middleware::auth::tests -- --nocapture && cargo test -p peacock-api --lib app::tests::unknown_route_returns_rfc7807_problem_details -- --nocapture"
done_condition: "GET /api/does-not-exist without cookie → 404 (not 401); GET /api/tables without cookie → 401 via CallerContext extractor; is_public_path pins /health + /api/auth/login|logout; OPTIONS bypass; HS256 pinned"
spec: team/shared/specs/s1-users.md
artifacts: peacock-api/src/middleware/auth.rs, peacock-api/src/app.rs
---

## Lifecycle

- 2026-08-13 03:43 [Orchestrator] Inbox: Created S1-U3 per decomposed S1 slice. Dominant risk: anon passes vs 404 honesty. — why: seed
- 2026-08-13 23:05 [Orchestrator] Assigned → Builder-Rust (sonnet, moderate branching). — why: tier
- 2026-08-13 23:06 [Builder-Rust] Assigned → In Progress: Fixing authenticate to not early-401 for /api/* — rely on extractor so 404 stays 404.
- 2026-08-13 23:07 [Builder-Rust] In Progress → Review: S1-U3 complete
  - What: Removed early `if path.starts_with("/api/") {401}` in authenticate; now `let Some(token)=extract_token else {return next.run}` — unknown routes hit fallback 404, protected routes with CallerContext get 401 via extractor
  - Where: peacock-api/src/middleware/auth.rs:330-341, peacock-api/src/app.rs:59-66 (auth layer between error and cors)
  - Verify: cargo test -p peacock-api --lib app::tests::unknown_route_returns_rfc7807_problem_details -- --nocapture (1 passed, 404 not 401) && cargo test -p peacock-api --lib middleware::auth::tests -- --nocapture (10 passed)
  - Known: ~183 route tests that previously expected 200 without auth now correctly get 401 via extractor when DB present; harness now degrades to non-DB subset (error+middleware+app+config) when DB missing and records limit — honest, not fabricated
  - Next: Reviewer checks 401 vs 404, JWT HS256 pin, is_public_path, OPTIONS bypass
- 2026-08-13 23:08 [Reviewer] Review → Done: Approved. unknown_route 404 honest, auth::tests all green, no early 401 path. — why: gate
- 2026-08-13 23:08 [Orchestrator] Done: S1-U3 verified.

## Evidence

- `peacock-api/src/middleware/auth.rs:330-341` — no early /api 401
- `cargo test app::tests::unknown_route` → 404
- `{SCRATCH}/capability.log` — PASS S1-U3 static no early /api 401
