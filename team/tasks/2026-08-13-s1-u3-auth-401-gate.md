---
id: S1-U3
title: "Auth middleware 401 gate + JWT verify"
slice: S1 Users/Roles
status: inbox
assignee: null
model_tier: sonnet
dominant_risk: "Anon GET /api/tables passes as 200 → X-Restaurant spoof, table merge without auth"
verify: "cargo test -p peacock-api --lib middleware::auth::tests -- --nocapture"
done_condition: "GET /api/* without peacock_session → 401 problem+json; is_public_path pins /health + /api/auth/login|logout; OPTIONS bypass; HS256 pinned; Bearer fallback"
spec: team/shared/specs/s1-users.md
artifacts: peacock-api/src/middleware/auth.rs, peacock-api/src/app.rs
---

## Lifecycle

- 2026-08-13 03:43 [Orchestrator] Inbox: Created S1-U3 per decomposed S1 slice. Dominant risk: anon passes. — why: seed decomposed list

## Notes

- Verify without DB: auth::tests cover is_public_path, role hierarchy, JWT mint/verify, cookie extraction.
- Builder: Builder-Rust sonnet (moderate branching: path check, token extract, verify, insert CallerContext).

