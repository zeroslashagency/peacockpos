# Review — S1-U2 ApiError::Forbidden 403

**Task:** S1-U2
**Reviewer:** Reviewer (opus, different role than Builder-Rust)
**Date:** 2026-08-13
**Result:** Approved

## Checklist

- [x] `ProblemKind::Forbidden` → 403, slug `forbidden`, title `Forbidden` — honest test `forbidden_maps_to_403` drives shipped code, not grep
- [x] `forbid_alias_maps_to_403` and `forbidden_status_reverse_maps_to_forbidden` pass
- [x] `require_role!` returns 403 (not 401) — `require_role_returns_403_not_401` and `require_role_manager_passes_for_owner_and_dev` pass, detail contains roles
- [x] No grep theater — tests call shipped `ApiError::forbidden` and `require_role!` on real `CallerContext`

## Issues

None.

## Approval

Review → Done — honest, not theater.
