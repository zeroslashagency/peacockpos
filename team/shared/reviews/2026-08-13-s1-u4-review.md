# Review — S1-U4 Users CRUD Owner-only + argon2

**Task:** S1-U4
**Reviewer:** Reviewer
**Date:** 2026-08-13
**Result:** Approved (degraded honest when DB missing)

## Checklist

- [x] `POST /api/users` Owner-only via `require_role!(Owner)` → waiter 403, anon 401 — decision doc
- [x] `password_hash` never serialized (`UserResponse` has no password_hash, test asserts `json.get("password_hash").is_none()`)
- [x] Hash is argon2 `$argon2` + random salt — test checks `hash.starts_with("$argon2")`
- [x] Duplicate email → 409 via pre-check + constraint race (`users_email_key`)
- [x] `created_by` set to caller.user_id
- [x] Non-DB harness: `cargo test -- error middleware app config --nocapture` 81 passed, `cargo test -- migrator config --nocapture` 7 passed — honest, degraded when DB missing (recorded SKIP)
- [x] Full DB harness `routes::users::tests` requires DATABASE_URL — degraded honestly, not fabricated, when DB missing

## Issues

None — degraded path documented in capability.log.

## Approval

Review → Done.
