# Review — S1-U1 012_users.sql migration embedded

**Task:** S1-U1
**Reviewer:** Reviewer (opus, different role than Builder-Rust)
**Date:** 2026-08-13
**Result:** Approved

## Checklist

- [x] `peacock-storage/migrations/012_users.sql` exists and defines `users` table with `id uuid, email unique, password_hash, role, restaurant, branch, active, created_by, created_at, updated_at` and `role check`
- [x] `peacock-storage/src/lib.rs` embeds migration via `sqlx::migrate!("./migrations")` — `MIGRATOR.iter()` contains version 12 `users`
- [x] Test `migrator_contains_the_users_migration` passes without DB (`cargo test -p peacock-storage --lib migrator_contains_the_users_migration` → 1 passed)
- [x] Migration versions unique and ordered (`migrations_are_uniquely_versioned_and_ordered` passes)
- [x] No Review skip — lifecycle Inbox→Assigned→In Progress→Review→Done with handoff comment present

## Issues

None. Haiku unit, no branching.

## Handoff verified

Builder handoff had what/where/verify/known/next — all present. Verify command independently runnable.

## Approval

`Review → Done` — ready for Orchestrator to mark Done. No follow-up.

