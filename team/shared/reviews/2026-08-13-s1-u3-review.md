# Review — S1-U3 Auth 401 gate + JWT

**Task:** S1-U3
**Reviewer:** Reviewer
**Date:** 2026-08-13
**Result:** Approved

## Checklist

- [x] `GET /api/does-not-exist` without cookie → 404 (not 401) — `app::tests::unknown_route_returns_rfc7807_problem_details` passes (404, problem+json, instance, request_id)
- [x] `GET /api/tables` without cookie → 401 via `CallerContext` extractor (not middleware early 401) — extractor gate honest
- [x] `is_public_path` pins `/health`, `/health/ready`, `/api/auth/login`, `/api/auth/logout`, OPTIONS bypass
- [x] `middleware::auth::tests` 10 passed (cookie extract, bearer fallback, JWT HS256 pin, hierarchy, public path)
- [x] No early `path.starts_with("/api/")` 401 — extractor is gatekeeper, 404 stays honest, existing routing tests keep passing

## Issues

None.

## Approval

Review → Done.
